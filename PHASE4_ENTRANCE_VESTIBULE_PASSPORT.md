# 4차 — 정문 안쪽 도착 구역 (Entrance Vestibule) Scene Passport

작성 2026-09-16. 대상은 열려 있는 `Kremlin_Grand_Palace_COMPLETE.blend`(저장 전).
범위는 **내부만** — 정문 바깥(y < −42)은 만들지 않는다.

## 문제 정의

대문 개구부는 6.06 × 9.2 m인데, 열면 바로 **폭 32 m · 깊이 28 m · 높이 12 m** 앞홀이 통째로 드러난다.
사이에 아무 단계가 없다. 그래서 지금은 "문이 열렸다"는 사건이 공간적으로 아무것도 만들지 않는다.
바닥은 홀과 같은 체커가 문턱까지 그대로 이어지고, 티테이블 T03이 문에서 5 m 앞에 놓여 있다.

설계의 핵심 장치는 **압축 → 해방**이다. 문 안쪽 6 m를 낮은 천장의 전실로 눌러 두고,
기존 기둥선 y = −36을 횡단 스크린으로 삼아 그 선을 지나는 순간 12 m 돔 공간이 열리게 한다.

## SCENE

- intent: 대문이 열렸을 때 드러나는 의전 도착 구역. 문지방 → 전실(압축) → 횡단 열주 스크린 → 앞홀(해방)의 4단 시퀀스를 만든다. 기존 사파이어·금장·대리석 어휘를 그대로 쓴다.
- deliverable: 편집 가능한 .blend 블록아웃 (저장은 사용자 요청 시), 검증 렌더
- units: metres; axes: right-handed Z-up
- render: BLENDER_EEVEE, 1920×1080, 24 fps, 1–250 (기존 유지)
- dynamic: 예 — 대문 개폐가 주 비트

## HIERARCHY

- 새 컬렉션 `50 | P4 - Entrance vestibule`
- 대문 리그는 `50 | P4 - Entrance vestibule` 안에 `RIG_gate_hinge_L` / `RIG_gate_hinge_R`
- 보호(건드리지 않음): `15 | COMPLETE - Monumental entrance facade`(정문 벽·파사드), `42 | P3 - Entrance wall and guards`, `11 | NEW HALF - Forehall architecture`(기초·측벽·코니스), `35`(돔 천장), `12`(상부 갤러리)
- 기존 `Royal double door leaf` / `.001`은 **삭제하지 않고** 힌지 엠프티에 부모로 물린다

## ASSETS

치수는 미터, 위치는 월드 좌표. 전부 `[BLOCK]` — 생성 크레딧 0, 추가 생성 없음.

| id | 이름 | route | fidelity | dimensions | location | role |
|---|---|---|---|---|---|---|
| A01 | `P4 threshold band` | BLOCK | stylized | 6.6 × 0.9 × 0.04 | (0, −41.4, 0.02) | 문지방 — 문 개구부 폭보다 0.5 넓은 사파이어 띠 + 금장 선 |
| A02 | `P4 floor medallion` | BLOCK | stylized | ⌀ 5.0 × 0.03 | (0, −38.6, 0.015) | 전실 중앙 바닥 메달리온(방사형 8분할), 도착 지점을 못박음 |
| A03 | `P4 vestibule soffit` | BLOCK | blockout | 22.8 × 6.0 × 0.55 | (0, −39.0, 5.85) | **압축 장치.** z 5.6~6.15 낮은 격자 천장. 12 m 천장 아래에 6 m 층을 만든다 |
| A04 | `P4 screen column` ×2 | BLOCK | stylized | ⌀ 0.62 × 6.0 | (±3.4, −36.0, 0) | 횡단 스크린 기둥. 기존 y=−36 그리드에 올림. x ±7.6은 티테이블 T03과 겹쳐 **의도적으로 비움** |
| A05 | `P4 screen arch` | BLOCK | stylized | 7.4 × 0.5 × 1.1 | (0, −36.0, 5.6) | 스크린 상부 아치 인방. 통과 개구 6.2 × 5.6 |
| A06 | `P4 ceremonial runner` | BLOCK | stylized | 4.0 × 11.6 × 0.02 | (0, −35.8, 0.01) | 문에서 스크린까지 축을 긋는 로열블루 벨벳 러너 + 금장 테두리 |
| A07 | `P4 guard plinth` ×4 | BLOCK | blockout | 0.9 × 0.9 × 0.45 | (±2.6, −39.6) / (±2.6, −37.2) | 근위 기사상 받침. 기존 `GEN_knight_armor_LOD` 인스턴스를 얹을 자리 |
| A08 | `P4 console` ×2 + `P4 console mirror` ×2 | BLOCK | blockout | 1.8 × 0.55 × 0.9 / 1.4 × 0.08 × 2.2 | (±9.0, −41.3) | 측벽 접견 콘솔과 타원 거울 |
| A09 | `P4 vestibule chandelier` | BLOCK | blockout | ⌀ 1.8 × 1.4 | (0, −38.8, 4.2) | 전실 전용 샹들리에. 돔 샹들리에(z 7.35~11.25)보다 훨씬 낮게 걸어 층을 분리 |
| A10 | `RIG_gate_hinge_L/R` + 기존 문짝 | EXISTING + BLOCK | — | 문짝 3.02 × 0.2 × 9.2 | 힌지 (∓3.03, −41.8, 0) | **대문이 지금 힌지가 없다.** 바깥쪽 모서리에 힌지를 세우고 문짝을 부모로 물린다 |

생성 자산: 없음. 크레딧 예상 0.

**기존 자산 충돌 1건** — `P3 tea T03 *`(티테이블, y −36.6~−35.4, x −7.6~−6.4)가 스크린 선 위에 있다.
스크린 기둥을 x ±3.4로만 두어 물리적 충돌은 피했지만, 의전 동선 바로 옆에 찻상이 놓인 배치는 남는다.
이동/제거는 **하지 않았다** — 기존 배치 변경은 승인 사항으로 남긴다.

## SHOT

- active camera: `CAM 21 | P4 - Gate arrival` (신규)
- framing/lens/target: 전실 안 (0, −33.5, 2.6)에서 대문을 되돌아보는 구도, 렌즈 28 mm, 타깃 (0, −41.8, 4.2)
- foreground: 러너와 메달리온 / subject: 열린 대문과 문지방 / background: 정문 벽 파사드와 낮은 소핏
- 기존 `CAM 04 | NEW HALF - Entrance facade`는 유지 — 홀에서 대문을 보는 기존 구도로 A/B 비교에 쓴다

## LOOK

- 재질 역할: 기존 것만 재사용 — `Ivory marble`(소핏·기둥·받침), `Sapphire stone`(문지방·메달리온 바탕·스크린 인방 인세트), `Antique gilded bronze`(금장 선·테두리), `Royal blue velvet`(러너)
- material route: 전부 `[EXISTING]`. 새 재질 **0개** — 역할이 이미 다 있다
- texture scale: 기존 월드 좌표 대리석 결 그대로 (P8a에서 넣은 것)
- 관계: 전실은 홀보다 **금장 비율을 낮게** 둔다. 압축 구간이 더 화려하면 해방이 죽는다

## LIGHTING

- focal subject: 열린 대문 개구부 / secondary: 바닥 메달리온 / 가장 어두워도 되는 곳: 소핏 아래 측벽 구석
- mood: 낮. 기존 `LIGHTING_Day` 프리셋 위에서 동작해야 한다
- environment route: `[EXISTING]` — 기존 world `P3 world day` 유지, HDRI 생성 없음
- key: 기존 `Daylight key`(3200 W, 반경 1.2, 좌상단)가 계속 구조 키. 전실 전용 키는 **세우지 않는다**
- fill: 전실은 소핏 때문에 자연히 어두워진다 — 그게 압축의 절반이다. 억지로 채우지 않는다
- practical: A09 전실 샹들리에가 유일한 전실 광원(모티베이션 있는 실광원). 실제 점광원 1개, 에너지는 렌더 보고 결정 — 돔 샹들리에보다 약하게
- 색관리: Khronos PBR Neutral, 노출 고정 (기존값 유지, 변경 금지)
- 감사: 전실이 앞홀보다 **어둡게** 나와야 한다. 같거나 밝으면 압축 실패

## MOTION

- fps/frame range: 24 / 1–250 (기존 범위 유지, 확장하지 않음)
- 주 비트: **대문 개폐** — f1 닫힘(rest) → f24 미세 흔들림(anticipation) → f40~110 양문 92° 개방 → f110~250 정지(settle)
- 힌지 회전축은 Z, 바깥 모서리 기준. 좌 힌지 +92°, 우 힌지 −92°
- 보조: 없음. 기존 윙 포털 문(30→78)·분수·CAM 12는 그대로 둔다
- rest frame: f1(닫힘)과 f150(완전 개방) 둘 다 정지 프레임으로 검수 가능해야 한다

## ACCEPTANCE

- **구조**: 신규 오브젝트는 전부 `50 | P4 - Entrance vestibule`에만 존재. 기존 컬렉션 오브젝트 삭제 0건. 소핏 z 5.6~6.15가 상부 갤러리(z 4.8~5.1)·천장(z 12) 어느 쪽과도 관통하지 않음. 문짝이 열릴 때 벽·기둥·문지방과 교차하지 않음
- **모션**: f1/f40/f110/f150에서 문짝 evaluated matrix_world가 실제로 변할 것. 회전 뒤집힘 없음. f110 이후 정지
- **시각**: `CAM 21`에서 문지방 → 러너 → 메달리온 → 스크린 개구가 한 축으로 읽힐 것. 소핏 아래 압축과 스크린 너머 해방이 값 차이로 구분될 것
- **조명**: 전실 평균 밝기 < 앞홀 평균 밝기. 소핏이 상부 갤러리에 그림자 누출을 만들지 않을 것. `LIGHTING_Day` / `LIGHTING_Night` 양쪽에서 렌더 확인

## 빌드 결과 — 설계 변경 6건

블록아웃을 세우면서 계획을 바꾼 지점. 전부 실측·감사에 근거한 변경이다.

1. **A03 소핏 깊이 6.0 → 2.4 m.** 계획대로 y −42~−36 전체에 z 5.575 소핏을 깔면 **높이 9.2 m 문짝이 뚫고 지나간다.** 힌지에서 문짝 끝까지 3.04 m이므로 90°에서 팁이 y −38.86까지 들어온다. 소핏을 y −38.4~−36.0으로 물려 **여유 0.46 m** 확보. 결과적으로 "낮은 천장의 방"이 아니라 **두꺼운 포털**이 됐는데, 압축 장치로는 이쪽이 더 낫다.
2. **A04 스크린 기둥 2 → 4본.** 포털이 깊어지면서 앞뒤 두 면(y −36.0 / −38.4)에 각각 기둥쌍이 필요해졌다. 통과 개구 6.18 × 5.02 m.
3. **A10 대문 리그 — 신규 제작 취소, 기존 리그 재사용.** 조사해 보니 `Entrance door hinge LEFT/RIGHT`가 **이미 존재하고 자식 21개씩(문 장식)을 달고 있었다.** 애니메이션만 없었을 뿐이다. 슬래브 `Royal double door leaf` / `.001` 2장만 이 리그에서 빠져 있어 다시 물렸고, 개폐 키는 기존 힌지에 걸었다. 지금은 **36개 부품이 함께 열린다.**
4. **신규 추가 — `P4 gate daylight backdrop card`.** 외부를 만들지 않기로 했으므로 대문을 열면 개구부가 **검은 공백**으로 드러났다. 정문 벽 바깥 y −43.2에 발광 카드(14 × 14 m, Emission 1.15)를 세워 주광으로 읽히게 했다. **이것은 외부 설계가 아니라 백드롭 치트다** — 실제 외부를 만들면 제거해야 한다. 이 때문에 "새 재질 0개" 원칙이 깨져 재질 1개가 늘었다.
5. **A06 러너 폭 4.0 → 3.0 m.** 4 m면 ⌀5 m 메달리온을 거의 덮어 방사 무늬가 안 읽혔다. 3 m로 줄여 양옆 1 m씩 드러낸다.
6. **CAM 21 두 차례 재배치.** 최초 (0, −33.5, 2.6)은 전실 샹들리에가 시선 축에 정확히 걸려 화면을 막았다. 최종 (6.8, −27.2, 3.1) → 타깃 (−0.8, −40.6, 3.0), 렌즈 28 mm 사선 구도.

### 감사 결과

| 항목 | 결과 |
|---|---|
| 문짝 최대 도달 y | −38.86 (포털 전면 −38.40, 여유 0.46 m) |
| 함께 움직이는 부품 | 36개 (f1 닫힘 / f110 개방 / f250 정지 확인) |
| 회전 뒤집힘 | 없음 |
| 신규 오브젝트 | `50 | P4 - Entrance vestibule` 컬렉션에만 존재 |
| 기존 오브젝트 삭제 | 0건 |
| 광원 추가 | `LGT_practical_vestibule_chandelier` 1개 (`LIGHTING_Day`) |

### 남은 과제

- 티테이블 `P3 tea T03`이 스크린 선 바로 옆(y −36.6~−35.4)에 남아 있다. 물리적 충돌은 없지만 의전 동선 옆 배치는 그대로다 — 이동 여부는 승인 사항.
- 포털 소핏 밑면이 렌더에서 어둡게 죽는다. 압축 의도상 맞지만 격자 무늬가 안 읽히므로, 소핏 밑면에 약한 보조광이나 금장 반사면이 필요할 수 있다.
- 블록아웃 레벨이다. 기둥은 원통, 샹들리에는 원반 프록시, 콘솔은 상자다. 디테일 패스는 별도.
- 밤 프리셋(`LIGHTING_Night`)에서는 아직 검수하지 않았다.

## refs_read

`blender-scene`, `blender-scene-spec`, `blender-modeling`, `blender-lookdev`, `blender-lighting-camera`, `blender-animation`
